#include<stdio.h>

int main()
{
	int a=0,b=0,mul=0;

        printf("Enter  first integer:");
        scanf("%d", &a);
        printf("Enter  second integer:");
        scanf("%d", &b);
        mul = a*b;
        printf("Multiply = %d \n", mul);
	
}
