#include<stdio.h>
#include<stdlib.h>
int main ()
{
        system("clear");
        int a,sq;
        printf("Enter the side length:");
        scanf("%d", &a);
        sq = a*4;
        printf("Area of this square is %d \n", sq);

        return 0;

}

